<h1>New Dose</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <% if @dose.errors.any? %>
    <div class="errors-container">
      Not cool…
    </div>
  <% end %>

  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit %>
<% end %>
